interface:
  display_name: "run-initiative"
  short_description: "Deliver one delivery branch through one review and final gate"
  default_prompt: "Use $run-initiative as the Delivery Worker for this large Ticket, Spec, or bounded Initiative."
policy:
  allow_implicit_invocation: false
