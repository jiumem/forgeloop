# 自动续配要求可证伪的新修复机制

三轮耗尽后的自动续配不能由措辞变化或等价重试触发。fresh Coder 必须引用证伪旧机制的证据，提出不同的因果假设，至少改变权威事实源、public Seam、状态转换、并发边界、算法、数据流、evidence producer 或验证入口中的一个实际干预维度，并声明可观察预测与能够证伪新假设的失败条件；新机制还必须保持在当前 Ticket Scope 内且不与历史机制语义等价。不满足该门禁时进入 `IMPLEMENTATION_BLOCKED`，不得自动获得新周期。
