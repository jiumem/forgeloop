## Agent skills

### Issue tracker

本仓库使用 GitHub Issues 管理 Spec、Ticket、依赖和运行状态。详见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用 Forgeloop 默认 Triage 标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用 single-context 领域文档布局。详见 `docs/agents/domain.md`。
