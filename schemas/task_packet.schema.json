{
  "$defs": {
    "PromotionPolicy": {
      "description": "晋级策略：定义任务完成后如何推进到下一阶段。",
      "properties": {
        "auto_promote": {
          "default": false,
          "description": "是否在 review clean 后自动推进（仍需人工签字）",
          "title": "Auto Promote",
          "type": "boolean"
        },
        "max_review_rounds": {
          "default": 3,
          "description": "review loop 最大轮次上限",
          "minimum": 1,
          "title": "Max Review Rounds",
          "type": "integer"
        }
      },
      "title": "PromotionPolicy",
      "type": "object"
    }
  },
  "description": "任务真值模型 — 四个核心模型之一。\n\n设计原则：尽量少，但必须足够执法。\n\n字段分层:\n- 身份层: task_id / title / phase / chain\n- 法源层: entry_docs\n- 范围层: must_do / must_not_do / done_criteria\n- 依赖层: depends_on / related_tasks\n- 验证层: required_checks\n- 推进层: promotion_policy",
  "properties": {
    "task_id": {
      "description": "任务唯一标识，如 P1_schema_baseline",
      "minLength": 1,
      "title": "Task Id",
      "type": "string"
    },
    "title": {
      "description": "任务标题，人类可读",
      "minLength": 1,
      "title": "Title",
      "type": "string"
    },
    "phase": {
      "default": "",
      "description": "所属阶段，如 M1",
      "title": "Phase",
      "type": "string"
    },
    "chain": {
      "default": "",
      "description": "所属任务链标识",
      "title": "Chain",
      "type": "string"
    },
    "entry_docs": {
      "description": "依据的文档路径列表",
      "items": {
        "type": "string"
      },
      "title": "Entry Docs",
      "type": "array"
    },
    "must_do": {
      "description": "必须交付的内容列表（至少一项）",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Must Do",
      "type": "array"
    },
    "must_not_do": {
      "description": "明确不做的内容列表",
      "items": {
        "type": "string"
      },
      "title": "Must Not Do",
      "type": "array"
    },
    "done_criteria": {
      "description": "完成标准列表（至少一项）",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "title": "Done Criteria",
      "type": "array"
    },
    "depends_on": {
      "description": "前置依赖任务 ID 列表",
      "items": {
        "type": "string"
      },
      "title": "Depends On",
      "type": "array"
    },
    "related_tasks": {
      "description": "相关任务 ID 列表（不构成硬依赖）",
      "items": {
        "type": "string"
      },
      "title": "Related Tasks",
      "type": "array"
    },
    "required_checks": {
      "description": "必须执行的验证命令列表",
      "items": {
        "type": "string"
      },
      "title": "Required Checks",
      "type": "array"
    },
    "promotion_policy": {
      "$ref": "#/$defs/PromotionPolicy",
      "description": "晋级策略"
    }
  },
  "required": [
    "task_id",
    "title",
    "must_do",
    "done_criteria"
  ],
  "title": "TaskPacket",
  "type": "object"
}
