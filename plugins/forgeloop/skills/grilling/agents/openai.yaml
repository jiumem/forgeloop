interface:
  display_name: "grilling"
  short_description: "Clarify independent decisions in bounded rounds"
  default_prompt: "Use $grilling to clarify this design in small dependency-aware rounds."
policy:
  allow_implicit_invocation: true
