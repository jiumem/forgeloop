# Forgeloop 3.2.0 发布候选

状态：发布前门禁已通过并取得用户发布授权；仅允许从 Release PR 合并后的最终 `main` Commit 发布。

## 发布范围

- 所有 `SHARED` Spec 由 Spec Root 承担唯一 Final Integration Gate，不再创建 ceremony-only 最终 Ticket。
- 每个共享 Spec 的 `spec_delivery_base` 在竞争 root Claim 前进入完整 Payload；合法零变更 Ticket 通过 `ALREADY_PRESENT` 映射。
- 四种共享分支原因复用一份只读门禁协议；累计审计只保留原生 PR/MR 特有规则。
- Final Gate Finding 通过既有 Pause 与显式 Repair Mode 路由；父合同变化统一返回 `CONTRACT_BLOCKER`。
- 不兼容旧 `Final integration owner` 声明，不提供 alias、parser、自动迁移或运行时回退。
- 20 个 Skill、11 个用户入口、9 个模型可调用能力及 Tracker/Git 原生架构保持不变。

## 验证证据

- 基线 Tag：`v3.1.0`。
- 源码门禁：Release Validator、Runtime Contract、Skill UI 元数据、上游同步与 Plugin Validator 通过。
- Fixture：GitHub、GitLab、Local 路径的 4 组运行矩阵通过。
- 测试：159 项 Python 单元测试通过；启用认证后 15 项 Agent 评测全部通过。
- 安装门禁：本地 `forgeloop-local` Marketplace 以 `3.2.0+codex.20260715074017` 重装。
- 安装回读：Installed Validator、Plugin Validator 与源码/缓存逐文件比对通过。
- 发布源码：候选验证后已恢复严格版本 `3.2.0`。
- 发布门禁：Release PR 合并、Tag/Release 同 Commit 与正式版缓存回读待执行。

## 发布命令

只在 Release PR 合并且本地 `main` 与 `origin/main` 同步后执行：

```bash
release_commit="$(git rev-parse origin/main)"
gh release create v3.2.0 \
  --repo jiumem/forgeloop \
  --target "$release_commit" \
  --title "Forgeloop 3.2.0" \
  --notes-file docs/releases/3.2.0-release-notes.md \
  --latest
```

Tag 必须指向合并后的最终 `main` Commit；不得从本地候选提交或未合并分支发布。
