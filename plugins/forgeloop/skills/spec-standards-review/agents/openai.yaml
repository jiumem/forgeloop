interface:
  display_name: "spec-standards-review"
  short_description: "Review bounded changes on two independent axes"
  default_prompt: "Use $spec-standards-review to review changes since this fixed point on the Spec and Standards axes."
policy:
  allow_implicit_invocation: true
