# Forgeloop 3.6.1 发布候选

状态：源码、测试与本地安装门已通过；目标模型完整 Agent evaluation 待在固定发布 Head 上执行。仅允许从 Release PR 合并后的最终 `main` Commit 发布。

## 发布范围

- 20 个 Skill 的显示名称与正式 Skill 标识完全一致。
- Codex 界面不再用动作别名替代 `to-spec`、`to-tickets` 等稳定名称。
- 插件运行包只包含 `.codex-plugin/` 与 `skills/`。
- 开发配置、Fixture、维护脚本和测试迁移到 `tooling/forgeloop/`。
- 发布校验拒绝开发资产与系统杂项文件进入插件根目录。
- Agent evaluation 明确旧 Run 与当前动作的布尔字段边界，不修改产品协议或期望动作。
- 20 个 Skill、11 个用户入口和 9 个模型可调用能力保持不变。

## 验证证据

- 基线 Tag：`v3.6.0`。
- 源码门禁：Release Validator、Runtime Contract、Skill UI 元数据、上游同步和 Plugin Validator 通过。
- 测试：205 项 Python 测试通过，11 项 authenticated Agent evaluation 将在固定发布 Head 上执行。
- 聚焦 Agent 验证：契约调和与目标漂移 4 项测试在 `gpt-5.6-sol` 下通过。
- 安装门禁：本地 `forgeloop-local` Marketplace 以 `3.6.1+codex.20260718110613` 重装。
- 安装回读：Installed Validator、Plugin Validator 与源码/缓存 Skills 逐文件比对通过；缓存顶层精确只有 `.codex-plugin` 与 `skills`；Codex CLI 回读插件为 installed、enabled。
- 发布源码：安装验证后恢复严格版本 `3.6.1`。
- 发布门禁：Release PR 合并、Tag/Release 同 Commit 与正式版回读待执行。

## 发布命令

只在 Release PR 合并且本地 `main` 与 `origin/main` 同步后执行：

```bash
release_commit="$(git rev-parse origin/main)"
gh release create v3.6.1 \
  --repo jiumem/forgeloop \
  --target "$release_commit" \
  --title "Forgeloop 3.6.1" \
  --notes-file docs/releases/3.6.1-release-notes.md \
  --latest
```

Tag 必须指向合并后的最终 `main` Commit；不得从本地候选提交或未合并分支发布。
