interface:
  display_name: "Diagnose Bugs"
  short_description: "Build a tight feedback loop and diagnose root causes"
  default_prompt: "Use $diagnosing-bugs to build a feedback loop and identify the root cause."
policy:
  allow_implicit_invocation: true
