interface:
  display_name: "双轴审查变更"
  short_description: "Review a fixed diff on independent Spec and Standards axes"
  default_prompt: "使用 $review-change 从固定点独立审查 Spec 与 Standards 两个轴。"
policy:
  allow_implicit_invocation: true
