# Forgeloop 3.0.0 发布候选

状态：等待用户单独授权发布。

## Breaking Change 摘要

- 活动套件由五个本地文档型 Skills 原子替换为 20 个 Tracker 驱动 Skills。
- GitHub、GitLab 与 Local Markdown 成为 Spec、Ticket、Blocking 与运行状态的唯一真理源。
- `run-initiative` 只接受正式 Spec 或持久化 Initiative 引用，使用独立 Coder、Spec Reviewer、Standards Reviewer、修复预算、集成与分层 Acceptance。
- 旧名称、历史产物处理、两条活动迁移路径和回退见 [迁移指南](../migrations/2.5.0-to-3.0.0.md)。

## 验证证据

- 2.5.0 基线 Commit：`a32f7a5d1eceace7215321dd7168dc7d07dac249`，可通过 Git 归档恢复五 Skill 源码。
- 活动目录：恰好 20 Skills；11 个仅显式调用，9 个模型可调用。
- Fixture：9 项入口/Tracker Fixture + 19 项 Runtime Fixture。
- 结构：20 次 Skill Creator、套件发布态、Plugin Creator、上游漂移、UI 元数据、运行协议、Python 单元测试与编译检查通过。
- 安装：本地 `forgeloop-local` Marketplace 通过 Cachebuster 重装；最终交付前恢复严格版本 `3.0.0`。
- 新线程：安装缓存使用 `--mode installed` 校验单一 `+codex.` 后缀；发现 20 Skills、11/9 Invocation Policy，并演练五个关键入口。

## 发布前复核命令

```bash
python3 plugins/forgeloop/scripts/validate_suite.py --mode release
python3 plugins/forgeloop/scripts/sync_upstream.py --check
python3 plugins/forgeloop/scripts/refresh_skill_metadata.py --check
python3 plugins/forgeloop/scripts/validate_runtime_contract.py
python3 plugins/forgeloop/scripts/validate_fixtures.py \
  plugins/forgeloop/fixtures/m1-tracker-paths.json \
  plugins/forgeloop/fixtures/m2-runtime-matrix.json
python3 /Users/nuc8/.codex/skills/.system/plugin-creator/scripts/validate_plugin.py plugins/forgeloop
```

## 已准备但禁止自动执行的发布命令

以下命令仅供取得用户发布授权后使用；先把 `<release-commit-sha>` 替换为获准发布的最终 Commit。

```bash
# 仅当 v2.5.0 尚无 GitHub Release 时，为既有 Tag 创建 Release。
gh release create v2.5.0 --verify-tag --title "Forgeloop 2.5.0" --notes-from-tag

# 从明确的发布 Commit 创建 3.0.0 Tag 与 Release。
gh release create v3.0.0 --target <release-commit-sha> \
  --title "Forgeloop 3.0.0" \
  --notes-file docs/releases/3.0.0-release-notes.md
```

未获授权时不得推送分支、创建 PR、打 Tag 或发布 Release。
