interface:
  display_name: "spec-standards-review"
  short_description: "Review explicitly requested bounded changes on two axes"
  default_prompt: "Use $spec-standards-review to explicitly review this bounded change set on the Spec and Standards axes."
policy:
  allow_implicit_invocation: true
