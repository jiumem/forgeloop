interface:
  display_name: "wayfinder"
  short_description: "Map and resolve one decision ticket at a time"
  default_prompt: "Use $wayfinder to map this large ambiguous objective into Decision Tickets."
policy:
  allow_implicit_invocation: false
