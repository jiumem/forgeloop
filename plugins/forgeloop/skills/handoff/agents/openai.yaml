interface:
  display_name: "Session Handoff"
  short_description: "Create a compact temporary handoff for a fresh session"
  default_prompt: "Use $handoff to prepare a compact temporary handoff for a fresh session."
policy:
  allow_implicit_invocation: false
