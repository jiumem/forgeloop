interface:
  display_name: "prototype"
  short_description: "Build a throwaway prototype to answer one design question"
  default_prompt: "Use $prototype to build a disposable experiment for this design question."
policy:
  allow_implicit_invocation: true
