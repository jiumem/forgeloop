interface:
  display_name: "解决合并冲突"
  short_description: "Resolve compatible merge intents and expose blockers"
  default_prompt: "使用 $resolving-merge-conflicts 恢复双方意图并安全处理当前冲突。"
policy:
  allow_implicit_invocation: true
