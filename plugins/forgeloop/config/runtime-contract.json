{
  "SKILL.md": ["At entry, read [scheduler.md]", "[events-and-recovery.md]", "continue both original Reviewers", "keeping every member Spec Open", "Only on Initiative `PASS`", "release the root Scheduler Claim", "Delivery is complete; Release was not executed by this Run"],
  "references/domain-and-state.md": ["Child thread identity is not durable state", "at most one active Ticket", "keep member Specs Open through Initiative Acceptance", "Delivery Acceptance", "Ticket Frontier, Spec Scope, and Initiative membership", "Do not create `PLAN.md`, `LEDGER.md`"],
  "references/scheduler.md": ["Role Task Pack", "exact same frozen inputs", "continue both original Reviewers", "reason=`REVIEW_BLOCKED`", "Repair Diagnosis Routing", "reported Head equals the Ticket Branch Head", "Do not advance the Frontier, Integration, Acceptance, or Closure", "Do not depend on a host-level close operation"],
  "references/coder.md": ["READY_FOR_REVIEW", "NO_CHANGE_REQUIRED", "CONTRACT_BLOCKER", "IMPLEMENTATION_BLOCKED", "Repair Diagnosis", "classification: LOCAL_REPAIR | STRUCTURAL_REPAIR | CONTRACT_BLOCKER", "Delivery Acceptance", "unrelated worktree changes"],
  "references/reviewers.md": ["read-only Reviewer", "Commit objects", "Delivery Acceptance", "REVIEW_BLOCKED", "finding_id", "withhold both results"],
  "references/events-and-recovery.md": ["Native envelope", "Prepared Literal Payload", "opaque literal data", "outside the repository worktree", "normalize CRLF to LF", "one record with an identical Payload", "whether their Payload bodies are identical or different", "ambiguous write result", "temporary preflight, not a new checkpoint", "atomically remove the current Ticket lock", "atomically remove `scheduler.lock`", "Do not rely on child thread existence"],
  "references/repair-and-integration.md": ["zero-write, zero-budget preflight", "at most three ordinary repair rounds", "candidate-caused Required Check failure", "reason=`CHECKS_BLOCKED`", "ALREADY_PRESENT", "READY_FOR_HUMAN_MERGE"],
  "references/acceptance.md": ["keeping all member Specs Open", "Only on Initiative `PASS`", "Delivery Acceptance", "Post-delivery Release Action", "owning_spec_ref", "Do not create a Ticket directly under the Initiative", "do not introduce a reopen state", "reason=`ACCEPTANCE_BLOCKED`"],
  "references/tracker-operations.md": ["Use `gh` for all operations", "`gh issue comment --body-file <payload-file>`", "Use `glab` for all operations", "`--field body=@<payload-file>`", "non-shell file operation", "one initial Coder result plus at most three ordinary repair results", "keep member Specs Open through Initiative Acceptance", "atomically remove only its owned Ticket lock", "owned `scheduler.lock`"]
}
